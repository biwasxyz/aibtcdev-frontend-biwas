import React from "react";
import DAOProposals from "@/components/reusables/proposal-mockups";

const page = () => {
  return (
    <div>
      <DAOProposals />
    </div>
  );
};

export default page;
